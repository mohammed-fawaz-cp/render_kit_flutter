## 0.1.1

* Support checking for both `build.gradle` and `build.gradle.kts` (Kotlin DSL) under `android/app/` in `doctor` command.

## 0.1.0

* Initial release of `renderkit_cli`.
* Validate environment, check dependencies, and perform builds.
